# Copy this file to .env.local and fill in the values.
# .env.local is git-ignored and should never be committed.

# URL of your deployed Puter worker (e.g. https://your-worker.puter.work)
VITE_PUTER_WORKER_URL=https://your-worker.puter.work
